Coloca aquí tu archivo `og-image.jpg`.

Cualquier archivo que pongas en esta carpeta `public` será accesible directamente desde la raíz de tu sitio web. Por ejemplo, `public/og-image.jpg` se podrá ver en `tusitio.com/og-image.jpg`.
